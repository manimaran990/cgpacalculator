<%- model_class = Grade -%>
<div class="page-header">
  <h1>About</h1>
</div>

<% if logged_in? and current_user.name=="manimaran" %>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:visualprog) %></th>
      <th><%= model_class.human_attribute_name(:spm) %></th>
      <th><%= model_class.human_attribute_name(:unixnetwork) %></th>
      <th><%= model_class.human_attribute_name(:middleware) %></th>
      <th><%= model_class.human_attribute_name(:xmlweb) %></th>
      <th><%= model_class.human_attribute_name(:vplab) %></th>
      <th><%= model_class.human_attribute_name(:unixlab) %></th>
      <th><%= model_class.human_attribute_name(:gpa) %></th>
      <th><%= model_class.human_attribute_name(:cgpa) %></th>
      <th><%= model_class.human_attribute_name(:tot_credits) %></th>
      <th><%= model_class.human_attribute_name(:tot_egrades) %></th>
      <th><%= model_class.human_attribute_name(:percentage) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @grades.each do |grade| %>
      <tr>
        <td><%= link_to grade.id, grade_path(grade) %></td>
        <td><%= grade.visualprog %></td>
        <td><%= grade.spm %></td>
        <td><%= grade.unixnetwork %></td>
        <td><%= grade.middleware %></td>
        <td><%= grade.xmlweb %></td>
        <td><%= grade.vplab %></td>
        <td><%= grade.unixlab %></td>
        <td><%= format("%.4f",grade.gpa) %></td>
        <td><%= format("%.4f",grade.cgpa) %></td>
        <td><%= grade.tot_credits %></td>
        <td><%= grade.tot_egrades %></td>
        <td><%= format("%.4f",grade.percentage) %></td>
        <td><%= grade.created_at.to_date %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_grade_path(grade), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      grade_path(grade),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
	   <h4> GPA calculator </h4><br>
	   <b> Source code:</b><br> <%= link_to "https://github.com/manimaran990/cgpacalculator", "https://github.com/manimaran990/cgpacalculator" %>
	   <br>
	   <br>
	   <h5>Under creative commons</h5>


<% end  %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_grade_path,
            :class => 'btn btn-primary' %>
